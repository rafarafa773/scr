# Simple C++ Reader
to compile is like this
```bash
g++ main.cpp -o reader -std=c++23 -O3 -march=native -flto=auto
./reader
```
to use run
```bash
./reader --help
```
still in development \
made in brazil
